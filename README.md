# microshell
A simple math-oriented CLI programming language built on Python.

# what's microshell?
Do I really have to repeat myself.

MicroShell is a simple math-oriented programming language built on Python. There is literally nothing more to say.

# questions that will likely be asked
## MicroShell.py doesn't work. what's wrong?
It depends on all the other files from this repository.

## what libraries does it use?
`socket` (for hostname), `os` (for clearing the screen), `time` (only `sleep()`)

## what are the use cases for this?
Uh, I don't really know. I guess it's a simple programming language to slightly get to know how to program? But then it lacks literally everything ever about a programming language. Well, you get me.

## why does `exit` print the help message and `help` exit the program?
Funny. Sake of the bit. Joke. You get it.

## why do commands such as `exit` and `exitmath` work in the scripts?
I'm telling ya, if there was a help command in Python, or any programming language ever to be honest, it would also work in a script. But Python developers made it a mostly script only language unlike [a certain someone](github.com/axxydr1xed), that's why it works.

---

boob
